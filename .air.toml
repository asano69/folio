[build]
  cmd = "make static/app.js && go build -o ./tmp/folio ./cmd/folio/"
  bin = "./tmp/folio"
  args_bin = ["server"]
  include_ext = ["go", "ts", "html"]
  include_dir = ["cmd", "internal", "src", "static"]
  exclude_dir = ["tmp", ".js"]
